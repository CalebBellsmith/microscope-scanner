# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Model files (large, trained locally) — they live in app/models/
model.pt
model.onnx
model.onnx.data

# Training data (images, kept local)
training_data/

# Scan output folders
slide_scan/

# Mac
.DS_Store
.AppleDouble

# Windows
Thumbs.db
desktop.ini

# ToupTek SDK (platform-specific, added locally) — lives in app/sdk/
toupcam.py
toupcam.dll
libtoupcam.dylib

# One-off helper scripts kept on the rig only
re_export.py

# Redundant source archive
abrasion.zip
