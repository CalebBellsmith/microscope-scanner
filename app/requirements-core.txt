# Core dependencies to RUN the scanner (capture + analysis, rules-mode quality
# check).  This is everything except torch/torchvision, which are only needed
# for the ML/hybrid classifier and for train.py.  Install those separately
# (pip install -r requirements.txt) if you want the ML path.
PyQt5>=5.15
opencv-python>=4.8
numpy>=1.24
pyserial>=3.5
Pillow>=9.0
mss>=9.0
scipy>=1.11
scikit-image>=0.21
openpyxl>=3.1
